some annoying things:

- converting the write shaders into ShaderMaterials does NOT work aka the behaviour between the current StandardMaterial3Ds and their converted versions is NOT the same (even if you do nothing to them post conversion) (this seems like a Godot bug)